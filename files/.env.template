# ============================================================
# saasmetrics.ai  |  .env
# Copy this to .env and fill in your values.
# NEVER commit .env to version control.
# ============================================================

# ── Gemini ────────────────────────────────────────────────
# Get from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Model for AI router (fast, cheap — Flash)
ROUTER_MODEL=gemini-1.5-flash

# Model for answer generation (best quality — Pro)
ANSWER_MODEL=gemini-1.5-pro

# ── GCP ───────────────────────────────────────────────────
# Your GCP project ID (e.g. saasmetrics-demo-123456)
GCP_PROJECT=saasmetricsai-demo

# BigQuery dataset name
BQ_DATASET=saasmetrics

# GCS bucket for user-uploaded files
GCS_BUCKET = saasmetrics-uploads-saasmetricsai-demo

# ── App ───────────────────────────────────────────────────
# Backend host/port
BACKEND_HOST=localhost
BACKEND_PORT=8000

# Max upload size in MB
MAX_UPLOAD_MB=20

# How many turns of history to pass to the model
HISTORY_WINDOW=12

# How many turns to pass to the router
ROUTER_HISTORY_WINDOW=4
